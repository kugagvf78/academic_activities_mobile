import 'package:flutter/material.dart';

class TinTucScreen extends StatelessWidget {
  const TinTucScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Trang Tin Tức')),
    );
  }
}